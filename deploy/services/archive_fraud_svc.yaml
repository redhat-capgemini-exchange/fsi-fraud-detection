apiVersion: v1
kind: List
items:
  - kind: DeploymentConfig
    apiVersion: apps.openshift.io/v1
    metadata:
      name: archive-fraud-svc
      labels:
        svc: archive-fraud-svc
        app-owner: fsi-fraud-detection
        app.openshift.io/runtime: golang
        group: integration-svc
        collect_metrics: "true"
    spec:
      selector:
        svc: archive-fraud-svc
      replicas: 1
      template:
        metadata:
          labels:
            svc: archive-fraud-svc
            app-owner: fsi-fraud-detection
            group: integration-svc
            collect_metrics: "true"
        spec:
          volumes:
            - name: datalake-archive
              persistentVolumeClaim:
                claimName: datalake
          containers:
            - name: archive-fraud-svc
              image: >-
                image-registry.openshift-image-registry.svc:5000/fsi-fraud-detection-xops/archive-svc:latest
              imagePullPolicy: Always
              envFrom:
                - configMapRef:
                    name: fsi-fraud-detection-config
              env:
                - name: source_topic
                  value: "tx-fraud-sim"
                - name: batch_size
                  value: 50

                - name: client_id
                  value: "archive_fraud_svc"
                - name: group_id
                  value: "archive_svc"

                - name: target_location
                  value: "/opt/app-root/data"
                - name: target_prefix
                  value: fraud

              volumeMounts:
                - name: datalake-archive
                  mountPath: "/opt/app-root/src/data"

  - kind: Service
    apiVersion: v1
    metadata:
      name: metrics-archive-fraud-svc
      labels:
        app-owner: fsi-fraud-detection
    spec:
      ports:
        - protocol: TCP
          port: 2112
          targetPort: 2112
          name: metrics
      selector:
        svc: archive-fraud-svc
      type: ClusterIP
